## 프론트엔드 리스크 관리


### 1. 컴포넌트 구조(부모-자식) 관련 리스크

- **문제**:  
  부모·자식 컴포넌트 구조로 인해 특정 화면 크기에서
  하위 컴포넌트에 적용된 스타일(`hidden`)을 인지하지 못해
  헤더가 화면에 나타나지 않는 문제가 발생

- **원인 분석**:
  - 상위 컴포넌트만 확인하고, 하위 요소에 적용된 스타일을 놓침

- **해결 방법**:
  - 컴포넌트 계층 구조 전체를 점검으로 부모 컴포넌트를 나와도 문제가 해결되지 않음을 확인
  - 하위 요소의 반응형 스타일 수정으로 문제 해결


### 2. API 연동 및 쿠키 접근 리스크

- **문제**:  
  배포 환경에서 API 연동이 정상적으로 동작하지 않으며,
  쿠키가 전달되지 않아 인증 관련 오류 발생

- **원인 분석**:
  - proxy 설정을 통한 API 요청과 실제 배포 URL을 통한 요청 방식의 차이로 인한 쿠키 접근 권한 문제

- **해결 방법**:
  - proxy 환경에서는 쿠키 접근이 가능하지만, URL 직접 접근 시 제한이 발생함을 인지
  - 백엔드와의 협업을 통해 요청 방식과 인증 흐름을 재정비하여 문제 해결


### 3. 카카오 로그인(OAuth) 연동 리스크

- **문제**:  
  카카오 로그인을 처음 구현하면서, 로그인 이후 URL에 포함된 `code` 값을
  프론트엔드에서 전달받아 처리해야 한다는 점. 또한, 배포 후 Firebase 환경에서 URL이 프론트 도메인과 다르게 나오면서 코드 값이 정상적으로 추출되지 않음

- **원인 분석**:
  - 프론트엔드 포트 번호와 도메인 개념에 대한 이해 부족
  - 배포 환경에서 오류가 프론트엔드 문제인지 Firebase 설정 문제인지 판단이 어려웠음
  - Firebase 배포 설정(`firebase.json`)과 백엔드 연동 시도 중 index.html 미설정으로 인해 접근 경로가 달라짐

- **해결 방법**:
  - 카카오 로그인은 **포트 번호가 아닌 등록된 도메인(redirect URI)** 기준으로 동작함을 이해
  - 로그인 이후 URL에서 인가 코드(`code`)를 추출하여 백엔드로 전달하는 구조로 수정
  - Firebase 배포 환경에서 `index.html`과 redirect 설정을 포함하도록 배포 구성 수정
  - 프론트엔드와 백엔드의 역할 및 OAuth 인증 흐름을 명확히 분리하여 문제 해결


### 4. 사용자 경험(UX) 리스크 (추후 관리)

- **리스크**:  
  운세 결과가 동일하게 반복 출력되는 구조를 사용자가 인지하지 못할 가능성

- **대응 방안**:
  - 추후 운세 1회 조회 이후 동일한 결과가 제공됨을 명확히 안내하는 문구 및 UI 표시 추가
  - 추후 운세 조회 완료 후에는 입력 페이지 접근을 제한하고, 결과 페이지를 중심으로 확인할 수 있도록 화면 흐름을 고정


### 5. 애니메이션 기능 반응형 리스크 (추후 관리)

- **리스크**:  
  화면 크기 및 해상도 차이에 따라 애니메이션 위치, 타이밍, 크기가 다르게 보여 UI가 어색하게 느껴지거나 의도와 다른 사용자 경험이 발생할 가능성

- **대응 방안**:
  - 애니메이션 적용 요소에 최소, 최대 화면 크기 기준을 설정하여 화면 크기 변화에 따른 오차 최소화
  - 실시간 화면 너비 값을 기준으로 애니메이션을 제어하여, 화면 크기별로 자연스럽게 동작하도록 구현
