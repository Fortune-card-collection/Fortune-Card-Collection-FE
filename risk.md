## 프론트엔드 리스크 관리


### 1. UI 반응형 및 레이아웃 리스크

- **리스크**:  
  다양한 화면 크기(모바일·태블릿·데스크톱)에서 레이아웃이 깨지거나 특정 컴포넌트가 숨겨져 사용자에게 정상적으로 노출되지 않을 가능성
- **대응 방안**:
  - 상·하위 컴포넌트 점검
  - 공통 레이아웃 컴포넌트 분리로 반응형 수정 범위 최소화
  - 레이아웃과 컴포넌트 크기를 제한하여 화면 축소·확대 과정에서 발생할 수 있는 반응형 UI 오류를 최소화함

---


### 2. 사용자 경험(UX) 리스크

- **리스크**:  
  운세 결과가 동일하게 반복 출력되는 구조를 사용자가 인지하지 못할 가능성,
- **대응 방안**:
  - 운세 1회 조회 이후 동일한 결과가 제공됨을 명확히 안내하는 문구 및 UI 표시 추가
  - 운세 조회 완료 후에는 입력 페이지 접근을 제한하고, 결과 페이지를 중심으로 확인할 수 있도록 화면 흐름을 고정

### 3. 애니메이션 기능 반응형 리스크

- **리스크**:  
  화면 크기 및 해상도 차이에 따라 애니메이션 위치, 타이밍, 크기가 다르게 보여
  UI가 어색하게 느껴지거나 의도와 다른 사용자 경험이 발생할 가능성
- **대응 방안**:
  - 애니메이션 적용 요소에 min / max 기준을 설정하여 화면 크기 변화에 따른 오차 최소화
  - 실시간 화면 너비 값을 기준으로 애니메이션을 제어하여, 화면 크기별로 자연스럽게 동작하도록 구현
